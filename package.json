{
  "name": "pi-async-compaction",
  "version": "0.2.2",
  "description": "Async compaction for pi sessions that does not block interactive input.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "async-compaction",
    "compaction"
  ],
  "license": "MIT",
  "type": "module",
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./extensions/async-compaction.ts"
    ]
  },
  "scripts": {
    "tsc": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.80.2",
    "@earendil-works/pi-coding-agent": "^0.80.2",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
